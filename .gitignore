# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/

# Env
.env
.env.local

# Node (web dashboard, future)
node_modules/
.next/

# Model weights / large binaries — never commit these
models/**/weights/
models/**/*.safetensors
models/**/*.ckpt
models/**/*.bin
*.mp4
*.mov
*.mkv
*.webm

# Local training-automation / training-data artifacts (job records,
# approvals, usage ledgers, ingested-dataset manifests, downloaded model
# weights) - always machine-local, never committed. See
# services/training/scripts/run_experiment.py, ingest_dataset.py,
# download_wan22_weights.py, and docs/adr/0024-wan22-real-training-backend.md.
.training-automation/
.training-data/
.training-runs/
.models-cache/

# Raw sourced video clips for training datasets (Stage 0/Dataset v1, see
# docs/DATASET_SPEC.md and dataset/sources/stage0_sources.md) - large
# binary media, sourced on a machine with real network access, never
# committed. Only the *.md sourcing docs under dataset/ are tracked.
dataset/raw/

# OS / editor
.DS_Store
.idea/
.vscode/

# Docker volumes
.docker-data/
