[project]
name = "auth"
version = "0.1.0"
description = "Provider-independent auth: IUserStore + IAuthProvider (local dev implementation, no external service)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/auth"]
