[project]
name = "director-memory"
version = "0.1.0"
description = "Per-project history of Creative Director pipeline artifacts, for revision loops"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/director_memory"]
