[project]
name = "storage-sdk"
version = "0.1.0"
description = "IStorageProvider abstraction for platform-owned asset bytes (local dev impl; S3/R2 later)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/storage_sdk"]
