{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.sallehly.ai/user.schema.json",
  "title": "User",
  "description": "Public view of a user - mirrors auth.User.to_public_dict() exactly. Never includes password_hash/password_salt; those never leave packages/auth.",
  "type": "object",
  "required": ["user_id", "email", "workspace_id", "created_at"],
  "properties": {
    "user_id": { "type": "string" },
    "email": { "type": "string", "format": "email" },
    "workspace_id": {
      "type": "string",
      "description": "The user's personal default workspace, assigned at registration."
    },
    "created_at": { "type": "string", "format": "date-time" }
  },
  "additionalProperties": false
}
