# Quality iteration log

Running, chronological log of every real Kaggle GPU generation used to
evaluate or improve output quality, per `eval/REPORT_TEMPLATE.md`. One
file per iteration, numbered in run order. Never edited after the fact
except to append new evidence - if a conclusion turns out wrong, the
next iteration says so explicitly rather than rewriting history.

| # | Title | Kaggle run | Verdict |
|---|---|---|---|
| 0001 | First real video, fp16 VAE decode | [30303715804](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30303715804) | FAIL - flat/muddy garbage frames |
| 0002 | fp32 VAE upcast fix | [30316220351](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30316220351) | FAIL - byte-identical to 0001, fix had no measurable effect |
| 0003 | guidance_scale 1.0 -> 6.0 fix | [30327666233](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30327666233) | FAIL - confirmed via SHA256 + direct visual inspection: byte-for-byte identical output to iteration 0001, despite +26% run duration. Fix had zero measurable effect; root cause still unknown. |
| 0004 | Metadata echo confirms guidance_scale=6.0 wiring is correct | [30331749263](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30331749263) | Diagnostic-only. Wiring confirmed correct via real diffusers source trace; root cause hypothesis narrowed to fp16 text-encoder overflow (T5-family models are well-documented to have this) - untested until iteration 0005 |
| 0005 | text_encoder fp32 upcast | [30333553288](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30333553288) | FAIL - 5th consecutive byte-identical output (127,355 bytes). text_encoder ruled out as the cause; only the transformer itself remains untested among fp16 submodules |
| 0006 | transformer fp32 upcast (last fp16 submodule) | [30336718604](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30336718604) | FAIL - 6th consecutive byte-identical output, no OOM. All fp16-precision hypotheses now exhausted. Added step_latent_norms diagnostic + configurable seed to test the denoising loop's real effect and input-sensitivity next |
| 0007 | seed 0 -> 42 | [30338955244](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30338955244) | CORRECTED - byte count/latent-norms genuinely changed (proving the pipeline is seed-sensitive), but direct visual inspection of the user-uploaded video disproved the "fixed" claim: still the same flat, textured, contentless output, just a different color cast. seed=0 was not uniquely degenerate - see 0008 |
| 0008 | Checkerboard artifact identified; disable VAE tiling | [30347507026](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30347507026) | OOM (13.45 GiB) - confirms tiling really was needed for memory at 17 frames, identical to the original iteration 0002 failure. See 0009. |
| 0009-0010 | Reduce frames to 9 instead of tiling | [30350022199](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30350022199) | HYPOTHESIS DISPROVEN - identical 13.45 GiB OOM at 9 frames as at 17 frames. The allocation does not scale with num_frames at all - likely resolution-dependent instead. |
| 0011 | Test resolution (480x272) as the OOM lever | [30352348205](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30352348205) | OOM avoided, but revealed something deeper: all 20 step_latent_norms are NaN from step 1 - a complete numerical failure, not just a checkerboard artifact. video.mp4 only 1,859 bytes. Model/scheduler mismatch ruled out by direct code audit (single from_pretrained call, no manual scheduler override). Testing tiling back ON at this same reduced resolution next to isolate the trigger. |
| 0012 | Re-enable tiling at the same 480x272 config (clean A/B) | [30353811714](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30353811714) | CONFIRMED: tiling ON -> all 20 step_latent_norms finite (261.85 -> 195.79); tiling OFF (0011, identical config otherwise) -> all NaN. Single-variable A/B proves tiling is required for numerical stability here, not just memory. Visual inspection: NOT the checkerboard - regular vertical blue banding, matching tile-blend seams (480px width / default 256px tile / 192px stride ~= 2 tiles). |
| 0013 | Force single-tile decode (tile size > frame size) | [30355470112](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30355470112) | NO NaN (60/60 finite, latent trajectory nearly matches 0012) - but a real confound surfaced: this run logged `bfloat16`, not the `fp16` assumed (never directly confirmed) for 0011/0012. Tile-count-doesn't-matter conclusion stands but is weakened, not settled. Visual check of the video (banding gone? real content?) still pending. |
| 0014 | Visual check on 0013's video + dtype confound resolved (no new Kaggle run) | same run as 0013 | Visually confirmed: 0012's vertical banding is gone, but output is still flat/muddy (avg stddev 8.84 < 20.0 threshold) - no recognizable content. Dtype confound resolved via reading real `torch.cuda.is_bf16_supported()` source: it returns True on T4 via software emulation, so bf16 was almost certainly the dtype in **every** iteration since 0001, not just 0013 - which retroactively explains why the 0002/0005/0006 fp32-upcast "fixes" had zero effect (their `if pipeline_dtype == torch.float16` guard likely never matched). Core flat-output cause still unknown; next test is empty-vs-simple-prompt to isolate text conditioning. |
| 0015 | 3-way prompt A/B (empty/"A red car"/"A snowy mountain") + a control re-run of 0013's own prompt | [30360209748](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30360209748), [30360289845](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30360289845), [30361353120](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30361353120), [30364299854](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30364299854), [30364516406](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30364516406) | **`prompt_embeds_norm`/`negative_prompt_embeds_norm` = exactly 0.0 in all 4 new runs (20/20 steps each), including a control run using iteration 0013's own prompt that had a real nonzero norm (41.77) in 0013 itself** - proves the zero-embedding result is not prompt-dependent. Latent trajectory and video size are now identical across 3 different prompts, self-consistently explained by CFG having nothing to differentiate if both conditional/unconditional embeds are zero. Root cause not yet isolated between (a) a real text-encoding bug or (b) an artifact of the `callback_on_step_end`+`enable_sequential_cpu_offload()` diagnostic instrumentation - next step is a direct, non-callback check of the encoder's output. Also found: GitHub Actions workflow_dispatch silently substitutes the declared default for empty *or whitespace-only* string inputs - no genuine empty-prompt test has run yet. |
| 0016 | Direct, non-callback measurement of `pipeline.encode_prompt()` (`torch.no_grad()`, before the denoising loop exists) | [30371065702](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30371065702) (OOM - diagnostic bug, missing `torch.no_grad()`, fixed), [30373730220](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30373730220) (success) | **CONFIRMED, not a callback artifact: `direct_prompt_embeds_min/max/abs_mean/norm` all read exactly `0.0`** - a direct, non-callback, no-grad call to WanPipeline's own real `encode_prompt()` shows the text embeddings are genuinely all-zero-valued, independent of `enable_sequential_cpu_offload()`/callback timing concerns. Candidate mechanism (not yet verified): `_get_t5_prompt_embeds`'s real zero-padding logic discards the encoder's output entirely if `seq_lens == 0`. Measure-only iteration per the user's instruction - no fix attempted yet. |
| 0017 | Trace every stage of `_get_t5_prompt_embeds`: tokenizer, attention mask, raw encoder output, seq_lens, final embeds | [30376466443](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30376466443) | **PINPOINTED: tokenizer and attention mask are both healthy (14 real tokens correctly identified, `seq_lens=14`, zero-pad branch does NOT trigger) - but the raw `text_encoder(...).last_hidden_state` output itself is already exactly all-zero before any slicing.** Same result for the empty-string negative prompt (1 real EOS token, healthy mask, zero raw output). Rules out the tokenizer, the attention mask, and the zero-padding logic as the cause - isolates the failure specifically to the text encoder's own forward pass. This directly explains the flat/muddy output since iteration 0001: zero text conditioning reaching the transformer. Measure-only per the user's instruction - no fix attempted; next step is checking whether `enable_sequential_cpu_offload()` or the encoder's own weights are implicated. |
| 0018 | Inspect `text_encoder`'s own weights directly: training mode, meta-tensor check, first parameter (`shared.weight`) stats | [30395402962](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30395402962) | Architecture/parameter-count healthy (243 tensors, ~6.73B total elements, `shared.weight` shape `[256384, 4096]` matches a real UMT5 encoder) and eval mode correctly active (`training_mode: false`) - but the first parameter is a **meta tensor** (`is_meta: true`, `device: "meta"`), meaning it has no materialized data at this measurement point. Honestly flagged as ambiguous: could mean the weights never loaded, or could be `enable_sequential_cpu_offload()`'s normal between-forward-calls resting representation (accelerate offloads a submodule's weights to a placeholder between uses). Measure-only per the user's instruction - the disambiguating no-offload test is recommended next but not yet dispatched, reported back to the user first as instructed. |
| 0019 | Clean single-variable A/B: same config as 0017/0018, `enable_sequential_cpu_offload()` disabled | [30398479042](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30398479042) | **DECISIVE: with offload OFF, `shared.weight` is real/non-meta (`is_meta: false`, norm `262741.84`, ~100% nonzero elements) - the checkpoint loads correctly - but `raw_encoder_output` is STILL exactly `0.0` (min/max/abs_mean/norm all zero), identical to offload-ON.** Rules out both `enable_sequential_cpu_offload()` and checkpoint-loading as the cause (weights are confirmed healthy either way). Run took ~76.5 min (vs. usual 13-15 min) without offload's staged transfers - itself a real, informative cost. Per the user's own explicit fallback for this exact outcome: stop pipeline-level experiments (tiling/resolution/dtype/offload all now examined) and investigate UMT5's forward computation / `transformers`+`diffusers` version compatibility directly. |
| 0020 | Isolate `UMT5EncoderModel` completely - no `WanPipeline` at all (CPU, after 3 real GPU memory failures) | [30431106646](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30431106646) (3 earlier GPU attempts failed: CUBLAS_STATUS_ALLOC_FAILED at load, a ~40 MiB-short OOM in the forward pass, CUBLAS_STATUS_ALLOC_FAILED at load again) | **FINAL: standalone `UMT5EncoderModel` (real class, real tokenizer, real checkpoint, `transformers==5.0.0`, no WanPipeline/diffusers/offload at all) still produces an exact all-zero `last_hidden_state`** (`min=max=mean=norm=0.0`, no NaN/Inf, shape `[1,226,4096]`). Three independent configurations (inside WanPipeline+offload-ON, inside WanPipeline+offload-OFF-weights-confirmed-real, and now fully standalone) all agree - the cause is inside `transformers`' UMT5 implementation/version for this checkpoint, not anywhere in this project's code. Standout candidate: checkpoint declares `transformers_version: "4.48.0.dev0"`, Kaggle has `5.0.0` (major-version jump). Measure-only per the user's instruction - next step (already built, `--transformers-version` flag) is re-testing with an older transformers pin. |
| 0021 | Same isolated UMT5 test, one variable changed: `transformers` pinned to `4.48.0` (the checkpoint's own declared version family) instead of Kaggle's `5.0.0` | [30432698242](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30432698242) | **CONFIRMED: root cause found. Under `transformers==4.48.0`, the exact same checkpoint/tokenizer/prompt/forward pass produces REAL embeddings** (`min=-0.855`, `max=0.949`, `mean=-0.0015`, `std=0.083`, `norm=79.73`, 925,278/925,696 elements nonzero, no NaN/Inf) - a clean flip from every prior `transformers==5.0.0` run's exact all-zero result (0016-0020). The all-zero text conditioning behind every flat/muddy video since iteration 0001 is a `transformers` version incompatibility (5.0.0 breaks `UMT5EncoderModel` for this checkpoint; 4.48.x does not), not this project's code. Measure-only per the user's instruction - pinning `transformers` project-wide and re-running a full `WanPipeline` video generation is the confirmed next step but awaits explicit user authorization. |
| 0022 | Baseline quality validation: 4 real full-pipeline videos (car/house/mountain/robot) after force-pinning `transformers==4.48.0` in both Kaggle kernel runners + pyproject.toml extras | [30440159420](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30440159420), [30441514431](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30441514431), [30442724517](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30442724517), [30444564540](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30444564540) | **CONFIRMED end-to-end: the fix holds inside a full real `WanPipeline` run.** All 4 prompts produced non-flat, prompt-matching video: `prompt_embeds_norm` genuinely varies per prompt (17.16-18.94, was a constant `0.0` before), video byte sizes differ (35-56KB, were byte-identical before), `avg_stddev` is 71-90 (threshold 20). Real decoded thumbnails (extracted+base64'd inside the GH Actions runner itself, since this session's network policy blocks the artifact-storage host) were visually inspected: a car, a house with mountains, mountain peaks, and a robot head are all genuinely recognizable and prompt-matching - a first for this entire investigation. Prompt-adherence scored 5-8/10 (weakest: C's "realistic documentary style" mountain came out painterly/abstract instead). Confirmed-working baseline config: 480x272, 9 frames, 20 steps, guidance_scale=6.0, seed=42. Also fixed a same-day workflow bug: `ffmpeg` isn't preinstalled on the GH Actions runner, which broke the new thumbnail-extraction step (now installed explicitly, both new steps marked `continue-on-error`). Hyperparameter sweep (steps/guidance/resolution/frames) proposed next, pending user go-ahead given the multi-hour GPU-time cost. |
| 0023 | Experiment: `num_inference_steps` sweep (20 → 30 → 40), one variable, same prompt A + golden baseline otherwise | 20 steps: [30440159420](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30440159420) (=0022), 30 steps: [30448674355](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30448674355), 40 steps: [30450561579](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30450561579) | **DECISION: keep steps=20.** `prompt_embeds_norm` identical across all three (confirms clean single-variable test). 30 steps: comparable visual quality to 20, no real improvement. 40 steps: `avg_sharpness` highest (6.174) yet the sampled frame is visually the *weakest* of the three - light streaks/indistinct shapes instead of a recognizable car, a real example of a pixel-stat proxy not tracking actual recognizability. Also the first real Kaggle run with the new CLIP prompt-adherence metric active: worked correctly (`clip_similarity_mean=0.259`), no prior baseline to compare against yet. Unplanned finding: actual encoded resolution came back `480x256`, not the requested `480x272`, in both 30 and 40-step runs - step-count-independent, not yet root-caused. Next: CFG/`guidance_scale` sweep (5/6/7) at steps=20. |
| 0024 | Experiment: `guidance_scale` (CFG) sweep (5 → 6 → 7), one variable, steps=20 + golden baseline otherwise | 5.0: [30452887238](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30452887238), 6.0 (=0022/0023): [30440159420](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30440159420), 7.0: [30455392303](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30455392303) | **DECISION: move the golden baseline's `guidance_scale` from 6.0 to 5.0** - not a marginal call. CFG=5.0 produced the clearest, best-composed result of the entire investigation (clean car silhouette, sharp taillights, legible city backdrop) and the highest CLIP score measured so far (`0.2684`). CFG=7.0 was a clear regression - blurry, oversaturated, car barely discernible - despite scoring the *highest* `avg_sharpness` (6.86) of the three, a second confirmation (after 0023) that the sharpness proxy can point the wrong way; `clip_similarity_mean` (0.2603) correctly tracked the visual regression instead. `prompt_embeds_norm` identical across all three confirms a clean single-variable test. New golden baseline: 480x272, 9 frames, 20 steps, **guidance_scale=5.0**, seed=42. Next: resolution sweep (480x272 vs 640x360). |
| 0025 | Experiment: resolution sweep (480x272 vs 640x360 vs corrected 640x352) + real root-cause investigation of the 480x256 rounding mystery | 640x360 (failed): [30469864044](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30469864044), 640x352 (corrected): [30478446581](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30478446581), 480x272 baseline (=0024): [30452887238](https://github.com/Y1X0/sallehly-ai-engine/actions/runs/30452887238) | **DECISION: move the golden baseline resolution from 480x272 to 640x352 - Baseline v1.0 frozen.** 640x360 failed with a real, deterministic `ValueError` (height/width must be divisible by 16 - 360 isn't) - not a memory limit as anticipated. Reading `WanPipeline.__call__`'s real source plus the checkpoint's own `transformer/config.json` (`patch_size=[1,2,2]`) and `vae/config.json` (`scale_factor_spatial=16`) found the *actual* silent constraint is `16x2=32`, fully explaining eval/reports/0023/0024's `480x256`-not-`272` mystery (`272//32*32=256`) with real arithmetic, no guessing. Corrected retry at 640x352 (352 is exactly divisible by 32, confirmed: `actual_resolution` == `requested_resolution`) produced the most decisive visual result of the entire investigation: for the first time the car's actual **red** color and full side profile are clearly visible, despite every raw pixel-stat metric (`avg_stddev` 33 vs 75, `avg_sharpness` 3.6 vs 5.4, `avg_saturation` 0.42 vs 0.77) reading *lower* than the 480x272 baseline - `clip_similarity_mean` (0.2977, highest of the whole investigation) correctly tracked the real improvement instead, a third confirmation that pixel-stat proxies can mislead. Generation time barely changed (~15 min vs ~14 min). **Baseline v1.0: 640x352, 9 frames, 20 steps, guidance_scale=5.0, seed=42.** Per the user's explicit redirection, the hyperparameter sweep stops here - next phase is a robustness benchmark across eval/benchmark_prompts.yaml's 30 categories at this frozen config, not further tuning. |
| 0026 | Full 30-category robustness benchmark at frozen Baseline v1.0 (640x352, 9 frames, 20 steps, guidance_scale=5.0, seed=42) - no hyperparameter changed between categories, only the subject prompt | 30 real Kaggle runs, one per category (see [0026 report](0026-robustness-benchmark-30-categories.md) for the full run-id table) | **CAPABILITY MAP, not a further tuning decision.** Mean CLIP across all 30 categories: 0.2997. Best 5: animals (0.3513), horror (0.3403), advertisements (0.3395), ocean (0.3312), fantasy (0.3296). Worst 5: close_ups (0.2508), mountains (0.2564), outdoor (0.2579), documentary (0.2705), cinematic_trailers (0.2722, flagged - CLIP disagrees with direct visual judgment). Weakness pattern isolated to two separable factors, each backed by counter-examples ruling out simpler single-axis theories: (a) repeated fine-grained geometry + deep perspective **combined** (not either alone - e.g. horror's hallway has deep perspective with no repetition and scored highest of the whole benchmark), and (b) a small/distant subject in a complex wide backdrop (documentary's eagle failed, but close-framed animals and anchor-less wide landscapes with a single strong focal point both succeed). One next experiment proposed, not started: targeted prompt engineering for architecture-heavy prompts (fewer repeated elements, one compositional anchor) before considering any fine-tuning. |
| 0027 | Phase 2: Architecture Prompt Study - 17 real Kaggle runs (4x Control + Groups A/B/C/D) testing whether 0026's architecture weakness is a prompting issue or a real model ceiling, at the same frozen Baseline v1.0 | 17 real Kaggle runs (see [0027 report](0027-phase2-architecture-prompt-study.md) for the full run-id table) | **DECISION: Case A - prompting issue, not a model ceiling. No LoRA/fine-tuning.** Control stayed byte-identical across all 4 measurements (CLIP=0.3474 every time), so every group comparison is trustworthy. Group A (concrete single-subject wording) won 3/3, clearly beating buildings (0.2915)/city (0.2813). Group C (compositional anchor) won 2/3 - fails only when the anchor's depth of field pushes the building into background blur (C.1). Group D (camera angle) showed aerial and extreme-close-up framing beat street-level. Key counter-evidence ruling out "any tweak helps": Group B (abstract style wording, e.g. "minimalist, clean lines") won only 1/3 and produced the single worst CLIP of the entire project (B.3, 0.2424). Recommendation: adopt a 3-principle Architecture Prompt Guide (name a concrete subject; keep anchor and building in the same depth of field; prefer aerial/close-up angles over street-level) - zero-cost, prompt-only, no training. `docs/KNOWN_LIMITATIONS.md` published as the practical capability reference. |
| 0028 | Phase 3B: Prompt Library Validation - rewrote 5 of report 0026's weakest categories (buildings, city, mountains, outdoor, documentary) per `docs/PROMPT_LIBRARY.md`'s 5 rules and re-ran each once at the same frozen Baseline v1.0, comparing against the original | 5 real Kaggle runs (see [0028 report](0028-phase3b-prompt-library-validation.md) for the full run-id table) | **RESULT: 4/5 validated (dual-signal: CLIP up + real visual improvement).** city 0.2813->0.3112, mountains 0.2564->0.302 (largest gain), outdoor 0.2579->0.2978, documentary 0.2705->0.2961 all validated (avg gain +0.0353). Only `buildings` failed to improve (0.2915->0.2913, flat) - traced to keeping the original's complexity language ("city skyline," "illuminated") instead of fully replacing it, not a library flaw. **DECISION: Prompt Library adopted, no LoRA/fine-tuning.** `docs/PROMPT_LIBRARY.md` updated with a "fully replace, don't layer" rule based on the buildings case. |
