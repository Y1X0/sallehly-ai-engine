# infra/kubernetes

Placeholder. Kubernetes is deliberately **not** used in Phase 0-6 — see
`docs/adr/0005-gpu-provider-runpod-vastai-first.md`. This directory will
hold Helm charts for a GPU node pool and a `KubernetesProvider`
(`IComputeProvider`) deployment once Phase 7 makes self-hosting worth the
operational cost.
