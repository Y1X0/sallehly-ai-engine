[project]
name = "creative-brief-parser"
version = "0.1.0"
description = "Raw user idea -> structured CreativeBrief"
requires-python = ">=3.11"
dependencies = [
  "llm-providers",
  "prompt-engine",
  "schemas",
  "jsonschema>=4.21",
]

[tool.uv.sources]
llm-providers = { workspace = true }
prompt-engine = { workspace = true }
schemas = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/creative_brief_parser"]
